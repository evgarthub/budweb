{
  "routes": [
    {
      "method": "GET",
      "path": "/navlinks",
      "handler": "Navlink.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/navlinks/count",
      "handler": "Navlink.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/navlinks/:id",
      "handler": "Navlink.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/navlinks",
      "handler": "Navlink.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/navlinks/:id",
      "handler": "Navlink.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/navlinks/:id",
      "handler": "Navlink.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
